services:
   nginx:
     image: nginx:latest
     ports:
      - 80:80
     restart: unless-stopped
     volumes:
       - ./html:/usr/share/nginx/html:ro
     networks:
      - homelab-1
networks:
  homelab-1:
    external: true
